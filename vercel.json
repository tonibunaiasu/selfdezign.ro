{
  "framework": "vite",
  "buildCommand": "pnpm run build:client",
  "outputDirectory": "dist/public",
  "cleanUrls": true,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
